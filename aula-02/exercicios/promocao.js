// Crie duas variáveis 
// valorCOmpra(numero)
// clienteFrequente (booleano)

// A promoção só é valida se o valor da compra for maior que 100 reais e o cliente for frequentes

let valorCompra = 120
let clienteFrequente = true
let promocao =  valorCompra > 100 && clienteFrequente

console.log(`A promoção é válida? ${promocao}`)