const pipoca = true
const chocolate = false

// Operador && (&&)
console.log(pipoca == true && chocolate == true )

// Operador ||(Ou)
console.log(pipoca == true || chocolate == true)

// Operador ! (Not)
console.log(`Pipoca: ${pipoca}`)
console.log(`Pipoca: ${!pipoca}`)